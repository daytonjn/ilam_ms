# file_names <- list.files(pi_sub_folder, #creates vector of file names
#                          pattern= "*.jpg",
#                          full.names = TRUE)
# load.image(file_names[1]) %>%
#   imsub(x %inr% c(x_left,x_right), y %inr% c(y_bot,y_top)) %>%
#   plot()
  
